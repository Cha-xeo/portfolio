'use client';

import React from "react";

// Preferred option in most cases (Arrow Function)
const MobyGameDetails:React.FC<number> = (id) => {
    return (
        <div>

        </div>
    );
}
export default MobyGameDetails;